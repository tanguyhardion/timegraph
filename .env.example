# Database Configuration (Drizzle ORM + Neon / Vercel Postgres)
# Leave empty for automatic local storage fallback during local development
DATABASE_URL=""
POSTGRES_URL=""

# ScrapingAnt API Key (Optional: built-in fallback parser operates if blank)
SCRAPINGANT_API_KEY=""

# Master Password & Auth Secret for Vault Security
# Set your private master password here (defaults to 'horology1815' if blank)
MASTER_PASSWORD=""
AUTH_SECRET=""

# Vercel Cron Security Secret (Optional: protects /api/cron/recheck)
CRON_SECRET=""
